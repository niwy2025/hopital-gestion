package com.hopital.patient.application.domain;

/** Documents supplied by, or captured for, a patient administrative dossier. */
public enum PatientDocumentType {
    PROFILE_PHOTO,
    IDENTITY_CARD,
    PASSPORT,
    BIRTH_CERTIFICATE,
    HEALTH_INSURANCE,
    REFERRAL_LETTER,
    OTHER
}
