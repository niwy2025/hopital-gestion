package com.hopital.patient.application.domain;

/** Nature of one immutable entry in a passage's clinical journal. */
public enum ClinicalEntryType {
    INITIAL_ASSESSMENT,
    CLINICAL_EVOLUTION,
    DISCHARGE_NOTE
}
